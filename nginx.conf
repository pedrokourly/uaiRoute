# Configuração Nginx para UaiRoute (Produção)
events {
    worker_connections 1024;
}

http {
    upstream frontend {
        server frontend:5000;
    }
    
    upstream backend {
        server backend:8000;
    }

    # Configuração principal
    server {
        listen 80;
        server_name seu-dominio.com www.seu-dominio.com;

        # Logs
        access_log /var/log/nginx/uairoute_access.log;
        error_log /var/log/nginx/uairoute_error.log;

        # Frontend (aplicação principal)
        location / {
            proxy_pass http://frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Backend API
        location /api/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Admin Django
        location /admin/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Arquivos estáticos
        location /static/ {
            proxy_pass http://backend;
        }
    }

    # Redirecionar HTTP para HTTPS (descomente se usar SSL)
    # server {
    #     listen 80;
    #     server_name seu-dominio.com www.seu-dominio.com;
    #     return 301 https://$server_name$request_uri;
    # }

    # Configuração HTTPS (descomente se usar SSL)
    # server {
    #     listen 443 ssl;
    #     server_name seu-dominio.com www.seu-dominio.com;
    #     
    #     ssl_certificate /etc/ssl/certs/seu-dominio.crt;
    #     ssl_certificate_key /etc/ssl/certs/seu-dominio.key;
    #     
    #     # Resto da configuração igual ao server acima
    # }
}
